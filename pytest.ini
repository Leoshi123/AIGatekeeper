[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

[coverage:run]
source = src
omit = */tests/*

[coverage:report]
exclude_lines =
    if __name__ == "__main__":
    pragma: no cover
